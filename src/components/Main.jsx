import React from 'react';
import styled from 'styled-components'
import './Main.css'

const Main = ({children}) => (
    <div className="Main">
            {children}
    </div>
);

export default Main;